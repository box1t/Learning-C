#include <stdio.h>

int main(){
    int _bullet;
    _bullet = 101;
    printf("This is me second assignment.\n");
    printf("The value of variable is: %d", _bullet);
return 0;
}

/* Ознакомился с типами данных, а также со спецификаторами для объявления переменных (которые и зависят от типа данных). */

#include <stdio.h>

int main() {
  int alfa=10;
  float beta=10.5;
  printf("The selling price was %d,\nbut it has grown up to %f", alfa, beta);
return 0;
}

/* Пока что косячу с символами. Запомнил, что после 'beta' не нужны большие кавычки.
Вводить текст по новой от руки выходит очень даже результативно, таким образом лучше усваивается материал. */

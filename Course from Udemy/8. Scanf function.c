#include <stdio.h>

int main(){
    int count;
    int amount;
    scanf("%d%d", &count, &amount);
    printf("The balance of count is %d, and the value of amount is %d.", count, amount);
return 0;
}
